using System.Text;
using Xunit;

namespace Librame.Extensions.Core.Tests
{
    //public class EncodingConverterTests
    //{
    //    [Fact]
    //    public void ConverterTest()
    //    {
    //        var converter = Encoding.UTF8.AsEncodingConverter();

    //        var str = nameof(EncodingConverterTests).AsByteBufferFromEncodingString();

    //        var buffer = converter.To(str);
    //        Assert.NotEmpty(buffer.Memory.ToArray());

    //        var source = converter.From(buffer);
    //        Assert.Equal(str, source);
    //    }

    //}
}
